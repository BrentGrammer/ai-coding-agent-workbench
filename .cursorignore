node_modules/
dist/
build/
.next/
coverage/
*.log
*.map

# Secrets and credentials. Cursor treats .cursorignore as best-effort, not a
# security boundary. Documented bypasses exist (Grep tool, git show on tracked
# files, terminal shell-outs). The reliable control is not mounting the secret.
.env*
!.env.template
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
.ssh/
.aws/
.gnupg/
.netrc
.git-credentials
**/credentials*
**/*secret*

# macOS
.DS_Store

# Git / VCS
.git/

# Python caches
__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Coverage reports
.coverage
htmlcov/

# Virtualenv: do not include installed packages in Cursor context/tools
.venv/

# Local AI/tool state
.cline/
.codex/
opencode.json
.clineignore
.geminiignore

# Serena MCP
# Keep .serena/project*.yml and .serena/memories/ visible.
.serena/cache/
# ignore this if you have secrets there and have sensitive config setup: .serena/project.local.yml
