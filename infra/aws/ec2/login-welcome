------ Welcome to the A.I. Assisted Cloud Workbench ------

You are ubuntu. Agents run as the agent user. Repos live in ~/workspace.

Clone with a Bitbucket access token:

  git config --global credential.helper 'cache --timeout=28800'
  clone-repo https://x-token-auth@bitbucket.org/WORKSPACE/REPO.git

You stage, commit, and push. The agent edits files but cannot touch .git.

Then start an agent:

  cd ~/workspace/<your-repo>
  agy
  start-pi --gpu-box
