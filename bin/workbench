#!/usr/bin/env bash
set -euo pipefail

echo "The AgentCore cloud path was removed (issue #20, Phase 1)."
echo "The EC2 replacement arrives as 'workbench ec2' in Phase 2."
echo "Until then, use the local Docker sandbox launchers (start-claude, start-codex, ...)."
exit 1
